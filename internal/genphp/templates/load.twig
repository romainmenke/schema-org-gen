<?php

define( 'SCHEMAORG_PLUGIN_PATH', dirname( __FILE__ ) );

require_once SCHEMAORG_PLUGIN_PATH . '/inc/util.php';
{% for file in files %}
require_once SCHEMAORG_PLUGIN_PATH . '/classes/{{ file }}.php';{% endfor %}
